{
  "name": "clean-session-curate",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@endorhq/capsule": "1.0.0"
  },
  "scripts": {
    "build": "npm run setup && npm test",
    "check-env": "node scripts/prepare_environment.mjs --check-only",
    "find": "node scripts/find_repo_sessions.mjs",
    "scrub": "node scripts/scrub_export_paths.mjs",
    "setup": "node scripts/prepare_environment.mjs",
    "test": "node --test"
  }
}
